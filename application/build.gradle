plugins {
    id 'java-library'
    id 'java-conventions'
}

group = 'com.kaua.file.processor.application'

dependencies {
    implementation(project(':domain'))

    testImplementation(project(path: ':domain', configuration: 'testClasses'))
}